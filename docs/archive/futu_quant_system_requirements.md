> **已归档** — 本文档为 Futu API 集成时的编码提示和需求说明。Futu 集成已完成，见 `src/collector/futu.py`。仅供历史参考。

# 角色设定
你是一个精通 Python 量化交易和富途 OpenAPI (Futu API) 的高级开发工程师。请帮我编写用于“美股及美股期权监测系统”的数据获取代码。

# 我的本地环境与权限状态
我已经成功在本地启动了 FutuOpenD（富途开放网关），具体的运行状态和权限如下，请严格基于我的权限来编写代码，不要使用我没有权限的接口（如 LV2 的买卖盘 OrderBook 等）：
- **API 监听地址**: 127.0.0.1:11111
- **使用语言**: Python (库名: `futu-api`)
- **订阅额度**: 300
- **历史 K 线额度**: 300
- **美股正股权限**: LV1 (正常，支持实时基础报价和历史 K 线)
- **美股期权权限**: LV1 (正常，支持期权链、期权实时报价)

# 核心任务需求
请为我编写一个结构清晰、带有详细中文注释的 Python 脚本，涵盖以下几个核心功能模块。代码需要考虑健壮性，包含连接检查和错误处理，并确保在程序结束时正确关闭上下文。

### 功能模块要求：
1. **连接网关**：创建 `OpenQuoteContext`，连接本地 FutuOpenD。
2. **获取正股快照**：单次拉取目标美股（如苹果 `US.AAPL` 和特斯拉 `US.TSLA`）的最新市场快照（Snapshot），打印最新价、成交量等关键信息。
3. **获取期权链**：以 `US.AAPL` 为例，演示如何获取它在下一个到期日（或指定日期）的期权链（Option Chain）数据，并过滤出 Call 和 Put 的代码。
4. **异步订阅实时行情 (重点)**：
   - 编写回调处理类（继承 `StockQuoteHandlerBase`）。
   - 订阅 `US.AAPL` 正股以及它对应的一个期权代码的实时报价 (`SubType.QUOTE`)。
   - 采用异步推送的方式，在控制台打印实时价格变动。
5. **获取历史K线**：获取 `US.AAPL` 最近 30 天的日 K 线数据（前复权），用于后续计算技术指标。

# 输出要求
1. 代码请组织为模块化或面向对象的形式，方便我后续集成到我的监测系统中。
2. 请在代码前后简要说明 Futu API 中 `subscribe` 订阅机制的注意事项（特别是关于订阅额度的扣除规则）。
3. 只需提供行情获取逻辑，暂不需要提供交易（Trade）相关的代码。