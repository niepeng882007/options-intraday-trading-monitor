version: "3.8"

services:
  playbook:
    build: .
    restart: always
    environment:
      - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}
      - TELEGRAM_CHAT_ID=${TELEGRAM_CHAT_ID}
      - TZ=America/New_York
    volumes:
      - ./config:/app/config
      - ./data:/app/data
